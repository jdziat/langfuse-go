# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of go coverage
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# Dependency directories
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
coverage.out
# Environment
.env
.env.local
dist/

# Example binaries built at the repo root (basenames of ./examples/* dirs).
# Root-anchored so they do not match the examples/ source files. The
# "evaluation" example basename is intentionally omitted because it would
# collide with the tracked root evaluation/ package directory.
/advanced
/basic
/llm_as_judge
/middleware
/testing
/v1_api

# Hugo build output
public/
resources/_gen/
.hugo_build.lock
